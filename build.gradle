apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.squareup:javapoet:1.7.0'
    compile 'com.google.code.findbugs:jsr305:3.0.0'
    compile 'com.facebook.fresco:fbcore:0.11.0'
}
